@model Nullable<DateTime>

@{
    DateTime? dt = DateTime.Now;
    if (Model != null)
    {
       dt  = (System.DateTime) Model;
       dt = DateTime.Parse(dt.ToString());
    }
    else
    {
        dt = null;
    }
   
    @Html.TextBox("", dt , new { @class = "date", type = "date"  })
} 


